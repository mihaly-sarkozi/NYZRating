-r requirements.txt

# Test runner és coverage
pytest~=8.3.0
pytest-asyncio
pytest-cov
trio

# HTTP/API testek
httpx~=0.28.1
respx

# Idő és external state mock
freezegun
fakeredis

# Lint / format / typecheck
ruff>=0.7.0
mypy>=1.11.0

# Offline névlista generálás (scripts/build_entity_gazetteers.py)
names-dataset>=3.0.0

# IDE remote debug (Docker / attach)
debugpy~=1.8.0
