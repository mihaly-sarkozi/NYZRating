title = "NYZRating Gitleaks Config"

[allowlist]
  description = "Tesztkörnyezeti dummy értékek"
  regexes = [
    "test-secret-with-enough-length-[0-9]+",
  ]
  paths = [
    "backend/tests/",
    ".github/workflows/ci.yml",
  ]
