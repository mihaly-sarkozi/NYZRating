# Architecture Docs

Rovid, operativ architektura dokumentumok:

- `outbox-worker.md`: outbox worker, lease/retry/heartbeat, internal health snapshot.
- `knowledge-ingest.md`: text/file/URL ingest flow, worker-first feldolgozas, status management.
